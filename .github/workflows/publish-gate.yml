name: publish-gate

# Re-runs the anti-leak gate and the framework's own contracts on every push and pull request.
#
# Mode is `ci`: the generic pattern layer only. The private layer lives outside this
# repository by design — a gate that shipped its own blocklist would publish exactly what it
# exists to hide. Full coverage runs on the authoring side before anything is pushed here.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install gitleaks
        run: |
          curl -sSL https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz \
            | tar -xz -C /usr/local/bin gitleaks
          gitleaks version

      - name: Run publish gate
        env:
          PUBLISH_GATE_MODE: ci
        run: bash scripts/publish-gate.sh "$GITHUB_WORKSPACE"

      - name: Verify the exit contract (the framework's own regression suite)
        run: |
          # The runner's default shell is `bash -e`; a fixture returning exit 5 is the
          # CORRECT contract behaviour, not a failure — disable errexit and manage the
          # verdict explicitly.
          set -u +e
          fail=0
          check() {
            bash sdd/bin/verify-gate.sh "sdd/fixtures/$1" >/dev/null 2>&1
            got=$?
            if [ "$got" = "$2" ]; then
              echo "  ok  $1 -> exit $got"
            else
              echo "  FAIL $1 -> exit $got (expected $2)"; fail=1
            fi
          }
          check DEFINE_FIXTURE_NEEDS_CLARIFICATION.md 5
          check DEFINE_FIXTURE_CONTROLE.md 0
          check DEFINE_FIXTURE_SEM_BLOCO.md 64
          # --strict must turn 5 into a non-zero, non-5 exit
          bash sdd/bin/verify-gate.sh --strict sdd/fixtures/DEFINE_FIXTURE_NEEDS_CLARIFICATION.md >/dev/null 2>&1
          got=$?
          if [ "$got" != "0" ] && [ "$got" != "5" ]; then echo "  ok  --strict -> exit $got"; else echo "  FAIL --strict -> exit $got"; fail=1; fi
          exit $fail

      - name: Playbook lint self-test
        run: bash sdd/bin/playbook-lint.sh --self-test

      - name: Skill frontmatter (Agent Skills spec)
        run: python3 sdd/bin/skill-lint.py .agents/skills/sdd-*/SKILL.md

      - name: Generator smoke (init is idempotent, sdd/ untouched by sync)
        run: |
          set -u +e
          T="$(mktemp -d)"; cd "$T" && git init -q && git config user.email ci@example.com && git config user.name ci
          bash "$GITHUB_WORKSPACE/bin/sdd-open.sh" init --yes --vendors claude,codex,gemini,cursor,kimi || exit 1
          git add -A && git commit -q -m baseline
          bash "$GITHUB_WORKSPACE/bin/sdd-open.sh" sync --yes || exit 1
          [ -z "$(git status --porcelain)" ] || { echo "sync was not idempotent"; git status --porcelain; exit 1; }
          bash "$GITHUB_WORKSPACE/bin/sdd-open.sh" sync --check || exit 1

      - name: Shell syntax, no zsh, no symlinks
        run: |
          set -u +e
          fail=0
          for f in $(find . -name '*.sh' -not -path './.git/*'); do bash -n "$f" || { echo "bash -n failed: $f"; fail=1; }; done
          if grep -rl '^#!/bin/zsh' --include='*.sh' . ; then echo "zsh shebang found"; fail=1; fi
          if [ -n "$(find . -type l -not -path './.git/*')" ]; then echo "symlink found"; fail=1; fi
          exit $fail
